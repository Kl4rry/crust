let failed = 0

for test in (echo tests/*.crust) {
    crust $test
    if $? != 0 {
        print "$test Failed"
        $failed += 1 
    }
}

if $failed == 0 {
    print 'Passed all tests'
} else {
    print "$failed tests failed"
}